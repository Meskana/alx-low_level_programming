#include <stdio.h>
/**
 * main - Execution starts here and main point too
 *
 * Return: Returns 0
 */
int main(void)
{
	char chi = 'a';

	while (chi <= 'z')
	{
		if (chi != 'q' && chi != 'e')
			putchar(ch);
		chi++;
	}
	putchar('\n');
	return (0);
}
